## 9.3 连接生命周期：握手、认证与心跳

OpenClaw Gateway 通过 WebSocket 长连接与远端设备进行持久通信。本节深入 WebSocket 连接的完整生命周期，包括握手、认证令牌交换、心跳保活、异常恢复，以及这些机制如何确保分布式系统中的可靠性。

### 9.3.1 为什么是 WebSocket

相比 HTTP 的请求-响应模式，WebSocket 长连接有三个优势：

1. **双向通信**：服务器可以主动推送消息给客户端（如“有新任务”、“请求批准”）
2. **低延迟**：无需每次都建立新连接、协议握手，延迟更低
3. **连接复用**：一条连接上可以并行处理多个逻辑流

### 9.3.2 WebSocket 握手与认证流程

当前 Control UI / WebChat 的握手不再是旧资料里常见的 `pairing_token -> session_token -> HELLO/HELLO_ACK` 两阶段流程，而是包含以下关键步骤：（1）WebSocket Upgrade 建立传输层连接，（2）Gateway 发出 **`connect.challenge`** 事件携带 `nonce`，（3）客户端生成/读取设备身份，（4）客户端用 `nonce` 签名后发送 `connect` RPC，（5）控制平面校验 token / allowedOrigins / 设备签名，（6）Gateway 响应连接成功，（7）客户端发送业务请求。只有第（6）步通过后，连接才真正进入可用状态。

```mermaid
sequenceDiagram
    participant UI as Control UI / WebChat
    participant Gateway as Gateway
    participant Control as Control Plane

    UI->>Gateway: [1] WebSocket Upgrade<br/>Origin: http://127.0.0.1:18789
    Gateway-->>UI: [2] event: connect.challenge<br/>{ nonce }
    UI->>UI: [3] 生成/读取设备身份<br/>deviceId + publicKey + privateKey
    UI->>Gateway: [4] req: connect<br/>token + device signature + nonce
    Gateway->>Control: [5] 校验 token / allowedOrigins / 设备签名
    Control-->>Gateway: ✓ 通过
    Gateway-->>UI: [6] res: connect ok
    UI->>Gateway: [7] req: status / chat / channels.status ...
```

图 9-1：WebSocket 握手与 challenge-response 认证流程

这个流程里，控制面会同时检查三类边界：

- **Gateway token/password**：由 `gateway.auth.mode` 决定。
- **Origin 白名单**：由 `gateway.controlUi.allowedOrigins` 控制。
- **设备身份签名**：要求客户端使用 challenge 中的 `nonce` 对连接载荷签名。

本次 live 日志里最典型的拒绝原因就是：

- `CONTROL_UI_ORIGIN_NOT_ALLOWED`
- `CONTROL_UI_DEVICE_IDENTITY_REQUIRED`

换言之：**WebSocket Upgrade 只是传输层建立，真正的认证发生在 challenge 之后的 `connect` RPC**。

### 9.3.3 当前令牌与设备身份的分工

按照当前 live 行为，Control UI 链路更像是“三层认证材料”协同，而不是“长期 pairing_token + 短期 session_token”的固定组合：

| 材料 | 生命周期 | 用途 |
|------|---------|------|
| **Gateway Token / Password** | 可轮换 | 证明调用者具备控制面访问权限 |
| **设备身份（deviceId + publicKey + privateKey）** | 本地长期保存 | 证明“这就是之前见过的同一设备” |
| **Challenge Nonce** | 单次握手有效 | 防重放，要求每次连接都重新签名 |

在浏览器/Control UI 场景里，设备身份通常保存在本地存储中；连接时先收到 `connect.challenge`，再用其中的 `nonce` 生成签名，最后把 `device.id`、`publicKey`、`signature`、`signedAt` 一起发给 Gateway。

这意味着当前排障重点也发生了变化：

- 不是先查“session_token 有没有过期”，而是先查 `Origin`、`Gateway token` 与 `device signature` 是否同时满足。
- 如果 token 正确但仍连接失败，往往是 `allowedOrigins` 或设备身份签名链路没有通过。

### 9.3.4 心跳与保活机制

在长连接中，网络中间件（代理、防火墙）可能因为空闲一段时间而主动断开连接。心跳机制用来防止这种“无辜掉线”。

- **传输保活**：当前更稳定的信号是 `hello-ok.policy` 中的 `tickIntervalMs`、后续 `tick` keepalive、`health` / `presence` 这类状态快照，以及 WebSocket 关闭事件。客户端应读取 `connect` 成功后的策略或实际配置，而不是写死本地常量。
- **业务心跳**：配置页里的 Heartbeat 是代理定时运行机制，用来周期性触发 agent turn；它反映的是业务巡检/定时任务，不是 WebSocket 断线检测本身，也不应和传输层 PING/PONG 超时混为一谈。

客户端侧更稳妥的伪代码如下：

```javascript
if (now - last_message_time > 60s) {
  // 实际阈值应基于 hello-ok.policy.tickIntervalMs 或部署配置推导
  connection.close(code=1000, reason="heartbeat_timeout");
  log_event("device_offline", device_id);
}
```

### 9.3.5 异常恢复与重连策略

设备与 Gateway 的连接可能因为多种原因断开：网络抖动、设备重启、Gateway 升级等。OpenClaw 设计了智能重连机制。

#### 重连的退避策略

设备应遵循指数退避（exponential backoff）：

```python
attempt = 0
while not connected:
    delay = min(
        base_delay * (exponential_base ^ attempt),
        max_delay
    )
    attempt += 1
    wait(delay)
    try_connect()
```

典型参数：

- `base_delay` = 1 秒
- `exponential_base` = 2
- `max_delay` = 300 秒（5 分钟）

#### 重连后的状态恢复

重连后，当前更准确的预期不是“自动 replay 旧事件并从某个公开 checkpoint 无缝续跑”，而是：

1. **重新 `connect`**：客户端再次完成握手，拿到新的 `hello-ok.snapshot` 与 `hello-ok.policy`。
2. **刷新状态**：如果发现事件序列缺口、连接中断或本地缓存不可信，客户端应按官方 runbook 重新拉取/刷新当前状态，而不是假设服务端会重放历史事件。
3. **重新订阅与恢复交互**：用户可继续使用同一 Gateway，但是否存在可恢复的业务状态，取决于对应会话/任务自身的持久化逻辑，不能把它写成 Gateway 协议层的统一承诺。

这样理解更稳妥：**控制面能帮助你重新建立连接并提供最新快照，但不公开承诺事件 replay、PostgreSQL 级 checkpoint 恢复，或 Agent 自动从断点继续执行**。

### 9.3.6 连接与会话的关键差异

初学者常混淆“连接”与“会话”的概念。这里澄清一下：

| 维度 | 连接（Connection） | 会话（Session） |
|-----|-----------------|--------------|
| **作用** | 传输通道 | 状态容器 |
| **生命周期** | WebSocket 连接建立 → 断开 | 用户任务开始 → 完成/超时 |
| **绑定关系** | 1 连接 ↔ 1 设备 | 1 会话 ↔ 多个连接（设备可重连） |
| **中断后** | 连接丢失，设备需重连 | 会话历史通常仍在，但应按能力边界重新获取快照/状态，不承诺自动续跑 |
| **数据存储** | 内存中（连接信息） | 持久化存储（会话历史、工具结果） |

### 9.3.7 本节小结

1. **握手阶段**：当前 Control UI 先收到 `connect.challenge`，再通过带 `nonce` 的签名 `connect` RPC 完成真正认证。
2. **认证机制**：当前关键材料是 `gateway.auth`、`controlUi.allowedOrigins` 与设备身份签名链，而不是旧资料里那种固定的 `pairing_token + session_token` 描述。
3. **心跳保活**：周期性发送 PING/PONG，防止代理中间件误断。
4. **异常恢复**：指数退避重连、会话状态持久化，确保服务连续性。
5. **概念区分**：连接是传输层，会话是业务层；设备可重连，任务不丢失。
